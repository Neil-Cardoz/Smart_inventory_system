# Smart_inventory_system
This implements a **Convolutional Neural Network (CNN)** based image classification workflow using the **CIFAR-10** dataset. The notebook is documented in a clean, GitHub-friendly style so it can be used directly in a repository as a reproducible project artifact.
